export default class Service {
  constructor(object) {
    this.id = object.id;
    this.title = object.title;
    this.descriptionOfService = object.descriptionOfService;
    this.price = object.price;
  }
}